# ================================
# ORION RAG SYSTEM DEPENDENCIES
# ================================
# 
# This project uses smart dependency management.
# Instead of manually editing this file, use the automated setup:

# RECOMMENDED: Interactive setup (detects your system)
# python set_dependencies.py

# AUTOMATED: Command line options
# python set_dependencies.py --auto --gpu --media --desktop --dev

# TRADITIONAL: Generate static requirements (if needed)
# python set_dependencies.py --generate-requirements --profile all

# ================================
# BASIC INSTALLATION (CPU-only)
# ================================
# If you prefer traditional pip install, here are the core dependencies:

# For CUDA 12.8 GPU support, install torch first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Core dependencies
ollama>=0.6.0
chardet
rank_bm25
langchain
langchain-community
langchain-ollama
langchain-text-splitters
sentence-transformers
chromadb
transformers
watchdog>=6.0.0

# PyTorch (CPU version - for GPU, see comment above)
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# Document processing
pandas
python-docx
python-pptx
openpyxl
pymupdf
pillow
striprtf

# FastAPI backend
fastapi
uvicorn[standard]
pydantic
python-multipart

# System integration / Utilities
pystray
psutil
tqdm
typer
rich
requests

# Features
faster-whisper

# ================================
# WARNING
# ================================
# This basic installation:
# - Uses CPU-only processing (no GPU acceleration)
# - No OCR/media processing capabilities
# - No desktop integration
# 
# For full features, use: python setup_dependencies.py
# To generate this file from `pyproject.toml` run:
# python set_dependencies.py --generate-requirements --profile core

# Optional profiles available: media, dev, nvidia-gpu, all
# Example: python set_dependencies.py --generate-requirements --profile all --gpu
